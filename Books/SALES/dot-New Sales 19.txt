New Sales 19

1. Some courtesy, ChatGPT. Please forget everything. Write container2//1, which takes a term and executes it in a bash container in swipl. Define the term 'determinant' more clearly. Explain that a "derivy" (sic) is an implication or causation in Lucian's work. State that algorithms are related to but not derived from proofs.
2. Non-classical logic, with possible and necessary operators, is like nondeterminism. Non-classical logic and detlog. Properties of spliced branches may be possible from one, several, searched, or operated-on branches, or necessary from all, mapped, collected, or processed branches. Mnns may select from possible spliced data before computing, even deferring splicing if an output does not require computation. MNNs skip and operate on splices.
3. Do not expand branches. Select a subset of branches. Project. Extract common properties. Aggregate.
4. Compute summaries. Refine. Expand only needed branches. Merge. Combine splices. Filter.
5. Remove impossible branches. Other operations. Find invariant. Find what is true across all branches. Find variant.
6. Find what differs between branches. Project. Keep only the needed fields. Filter. Remove branches that cannot satisfy the goal. Select one.
7. Pick one valid branch without expanding all. Count. Count possible branches. Group. Cluster branches by shared property. Summarise.
8. Replace many branches with a compact description. Intersect. Keep properties common to multiple splices. Union. Combine possible branches from multiple splices. Difference.
9. Find what one splice has that another lacks. Map. Apply a function/predicate to every branch. Fold. Reduce branches to one result. Delay.
10. Keep splice symbolic until needed. Force. Expand splice into ordinary answers. Memoise. Cache expensive branch results. Rank.
11. Order branches by usefulness/cost/probability. Cut impossible. Remove branches contradicted by constraints. Explain. Return why a branch/property is possible or necessary. MNNs have only invariants; they can compute variants from data.
12. Flatten predicate hierarchies by treating dependent variables as indices. The topological graph slices are not the representation used after finding invariants. They are the mechanism used to discover the invariants. Data. Graph. Topological graph slices.
13. Invariant discovery. MNN. Not:. Data. MNN operates on graph slices forever. For example, suppose you have:
14. a :- b,c. a :- b,d. a :- b,e. A predicate hierarchy sees: a
├─ b,c
├─ b,d
└─ b,e
15. A graph slice through the dependency graph immediately shows: B. Appearing in every branch. The invariant is:
16. a :- b, X. X :- c. X :- d. X :- e. Or in your terms: b. This is the invariant level.
17. The variants: c, d, e. Remain at the data level. Similarly, if you have [1, 3].
18. [1,4]. [1,5]. A graph slice across corresponding positions reveals: Position 1 = always 1. Position 2 = varies. The invariant:
19. [1,_]. Goes into the MNN. The variants: 3,4,5. Stay at the data level. Then, manipulate, and… the invariant levels (heuristic logic).
20. Generalise. Create a broader invariant. Specialise. Introduce controlled variants. Merge. Combine compatible invariants.
21. Split. Separate incompatible invariants. Rank. Score invariants by usefulness. Predict. Suggest unseen invariants.
22. Propagate. Push an invariant through a graph. Compress. Replace many invariants with one. Refactor. Rewrite using higher-level invariants.
23. Search. Find invariant combinations. Compose. Build larger invariants from smaller ones. Explain. Generate human-readable reasoning.
24. Prioritise: think of aims, the world, and logic when rewriting rules. Level 0 Facts. Level 1 Variants. Level 2 Invariants. Level 3 Invariant transforms. Level 4 Heuristics.
25. Level 5  Goals. Level 6  World knowledge. Level 7  Logic over goals and world knowledge. Level 8  Rule rewriting decisions. Think of a good reason why the world's food supply is running out, because of the impending ice age. How could robots prevent the doomsday event?
26. Ideas for manual (non-statistical) approximations of regression. Two-point line. Geometry. Balancing. Mechanics. Difference pyramid.
27. Numerical analysis. Decision tree. Algorithms. Invariant extraction. Logic. Grammar regression.
28. Compression. Splice regression. Concurrent explanations. Macroproof regression. Theorem proving. Pigdetlog runs a predicate if it knows the variables with high probability, based on their splice data.
29. Calculate splice probability from the probability within a list and splice. Schedule expensive predicates in regularly available slots. Explain how a:- b,!,c. works with Detlog and identifies cases that are too complex to work on. Simplify cut to deterministic logic to meet robot and real-time requirements.
30. Try harder to find methods to code deterministic cuts. Treat the piglets in Piglog as the threads or predicates of the algorithm running simultaneously. Run a predicate when some variables are known. Mark which constraints are known. Mark which probabilities are known.
31. Invariants are known. Possible values are known. Readiness. Information available. Action. 0.
32. None. Idle. 1. Types/invariants. Compile or specialise the predicate. 2.
33. Constraints. Propagate constraints, prune impossible branches. 3. Probability distribution. Speculatively execute likely branches. 4.
34. Use the information available. None. Idle. 1. Types/invariants. Compile or specialise the predicate. 2. Constraints. Propagate constraints, prune impossible branches. 3. Probability distribution. Speculatively execute likely branches. 4. Partial variable bindings. Partially evaluate the predicate. 5. Complete bindings. Produce definitive results. Prepare for the Prolog program requirements.
35. I synthesised decision trees from Spec to Algorithm, Starlog, Loop2, Plop, Detlog and Piglog. Decision trees from Spec to Algorithm - merge clauses, and attempt to run them in this way for better performance. Starlog - syntactic optimisation. Loop2 - converts findall to loops. Plop - semantic optimisation with necessary variable output, subterm indexing, subterm index looping, formulas with Gaussian elimination and memoisation. Detlog uses the splice predicate to perform nondeterminism.
36. Piglog merges predicates, running them simultaneously for better performance. They are synthesised by merging predicates and clauses, converting findall and formulas, and performing semantic optimisation, using splice for examination, fast nondeterminism, and Starlog for syntactic optimisation. If anything, a single command that converts the entire conversion pipeline is missing. All converters should be interpreter modules. To have citations. Philosophy of Computer Science articles.
37. In terms of the report of Computer Science and sentences that are syntheses of ten sentences. Computer science/philosophy format. 30 pages. 15, not 10, sentences for the main writer. X add egs to the Socratic analyses. Do 20-level details for 10 points; the computer does the rest.
38. Research ZPD tracking, including comments and connections, by checking whether students enjoyed understanding a point and the connection between comments. Encourage discussion and reflection in one-minute exams after lectures, and increase enjoyment and examination of the material. Build an invariant formal logic chatbot. Create invisible bugless methods. Design a chatbot that suggests interesting methods of joining algorithms.
39. Using an expert system. Snake data flows through existing preds. Old idea of linked specs in dictionary. Reconnect specs, which are in terms of subterm with address, etc. Can do extreme optimisation up to and after recording dictionary items. It should check it, redo it, and find the exact solution.
40. Instead of an attention framework, it finds steps in order, fills in missing steps, and makes corrections using an expert system. Instead of interpolating data flow, it inspects query terms and builds or modifies the solution. Uses expert algorithms in dict to construct solutions. May not mix algs, just runs one or a chain. Finds interdisciplinary solutions even when not conventional. Area of study: expert systems.
41. Write long algorithms, inferring and using initiative to complete them. Do not fear induction or writing chatbots. Change the following sentences to Prolog browser menu items: Find methodologies and paths between them in terms of sentences. Connect points, but keep data about conns. Connect points. Use interfaces and specs. Generate sentences by agent-writing agents.
42. Have a chatbot author a chatbot and produce work.

43. Detlog - explicit conditions from nondeterminism. Reuse code. Without variables. The splice predicate, etc., applies to simple cases and is not designed to make code more complicated. Cut needs to be simplified to deterministic logic to meet robot and real-time requirements. Record taxonomies and problematic conversion cases.
44. Chatbot agents may define optimal output as not overdescribed and divided into thought chunks, with something interesting to think about at each point. Why can't the high-quality secret computation, or another example, be made explicit at each point, at least referred to? Piglog unknown variables from tables of previous values. Lucian Academy should automatically detect disk-mounted departments and convert text files to Essay Helper files. Piglog runs segments in parallel by finding segments based on completed variable unifications, predicting the best choice, or revealing the path based on data predictions. Ideas for manual (non-statistical) approximations of regression.
45. By taking the result of a predicate, other predicates that need this value can run in parallel to improve performance. When would some "non-humans" run predicates in parallel, even without variables being defined yet? Piglog (sic) activates predicates when enough variables are available, or some variables are known. Constraints are known. Probabilities are known.
46. Invariants are known. Possible values are known. 
47. How can Detlog be run on a 6502? 
48. Finding multiple clauses and performing unification require their own compiler. 
49. The compiler and converter should be run one at a time. 
50. Algorithms can be divided into the least-invasive parts or loops. This may require loop syntax that allows pausing, saving and continuing.
51. The compiler would need to be well-designed to cope with memory limitations if it compiled nondeterminism deterministically. Optimisation helps, but keeping data to splice is not as favourable as splicing as one goes, or optimising delayed splicing. Detlog is likely to have lower memory overhead than Prolog. Piglog can also run algorithms by executing its predicates in parallel, predicting data and choices, thereby improving performance. Codegen learning. The Prolog browser chatbot presents a chat menu with auto-completed queries.
52. Just answers questions. If there is a part to be modified, it does it. If there is a ". " before ". " in Prolog, delete it. If there is a syntax error or repeated include, delete it.
53. Inspect, in other words, neurospect an algorithm with manual annealing to optimise it. Given input, output, and context, with types and patterns of types and characters, neuromatches to an algorithm. Convert list of specs to grammars. Data is converted to the appropriate type if it is any of those types. Data can have invariants (constants). If given user data, it shouldn't delete the production database.
54. It is possible to claim that, with Context-Free Grammar Generators and predicate generators, a form of artificial General Intelligence has been achieved. Nondeterminism simulated as determinism allows natural expression speed and allows longer computations to run instantly. Reoptimise algs with general enough breadth to include useful cases. This breadth should be sufficient to avoid mistakes based on prior knowledge and to be respectful of local or global frontiers of knowledge. Global approaches to knowledge need to be respected in the new relationships they uncover, and the implications of the reduced topology of knowledge for other conclusions need to be finalised for all knowledge. This conjecture can’t be done by me using my systems.
55. Chatbot. A programming language to produce a program. Cfg generator as a central idea in a chatbot. Chatbot checks using expert systems. Physics checked against. Physics is included, like chemistry and biology.
56. Race to replace mathematics with AI with spec to algorithm. Spec to Algorithm, PLOP and theorem provers, using S2A to find macroproofs, fill gaps and insert proof sequences. S2A inserts methodologies for IO proof specs. Cp to loops and spliced output- classes define well-writtenness and whether nondeterminism can be refactored simply without lone choice points and backtracking between predicate branches can be made into a single predicate with loops and optionally run expensive predicates. Choice points to loops and spliced output-instead of tabling, edit or morph the repeated clause. How can general cases from multiple algorithms help propose laws?
57. Robots-minimal set of everything such as Text-to-Breasonings, Grammar Logic, Time Travel to immortality simulation, Spec to Algorithm, optimisations. Loop2 arg ideas-ways of refactoring or writing without extra choice points, using compression, or cfg generators and non-brute-force algorithms. Spec to Algorithm’s code generator (using Combination Algorithm Writer, dictionary data) also converts sentence specs into data, then into code using S2A and generators. Tests are changed. Lucian CI/CD tests and modifies code in response to test changes. Predicates are merged.
58. Spec to Algorithm needs to be modified to handle greater than 14 items at a time, possibly with a manual neuronet. Logic also needs to be applied to generate code, change tests, make changes, test, and debug code in precise, natural language. Lucian CI/CD finds code using logic, in and between predicates. I need Prolog because it produces cognitive code. I need Detlog because it combines the syntax of Prolog with even greater simplicity of fewer compiler instructions. While a(1).
59. A(2). Becomes a([1,2]). In Detlog, clauses remain clauses and are collected by findall rather than being converted to a list by the converter, allowing them to be processed by higher-order algorithms. Member with findall of spliced output, abstracted out by the converter. Detlog is the only way to run Prolog on an Apple IIe. Detlog simulates cuts.
60. Cut mult. Context-Free-Grammar generator grammars, maths. Cut in member_cp. End of pred. Cut Object Hamlet or integration multiplication in Conglish. MNN and logic.
61. Integration. Sane. Detlog comments to show findall trace. Detlog allows examination of nondeterminism through the deterministic splice predicate. Logic in member cps allows subterm indexing and looping through them. Logic is like neuronet content, a single starlog command to produce output.
62. Neuronets might optimise logic by skipping B in transitivity, for example, A to B, B to C, gives A to C. Perhaps delete the h1a line. Include('. Philosophy/s2a/spec_to_algorithm.pl'). Delete.
63. Writeln(Out),. Or is it? Writeln(G2). Include query with alg. H1a helps uncover the meaning of args for a topic through spellings and uncover how to write a mind mapping alg. Detlog.
64. Assertz. Recompile new preds, x. Instead, place in structures if data or algs. Higher-order programming. Recompile the X chain command. MNNs.
65. MNNs are just the algorithm, with others, optimised and self-evident inferences drawn using NNs or MNNs aimed at laws, methodology levels and domains. Process command terms and run commands (higher-order programming) by simulating or transitively predicting these. Flatten hierarchies of preds. Flatten higher-order programming hierarchies to Apple IIe chain command to use less memory. Oliver Tree sim. Derivy” means “something generated from something else.
66. Proofs do not always correspond to algorithms. Proofs are algorithms with types. Non-classical logic and detlog. Properties of Spliced branches may be possible/from one/several/searched/operated on branches or necessary/from all/mapped/collected/processed branches. MNNs may select from possible spliced data before computing, even deferring splicing in case an output may not require computation. Example of skipping splices in detlog with MNNs: If 1 appears with a,b more often, check 1’s first.
67. MNNs skip and operate on splices. Yes. In your framework, one of the main purposes of an MNN could be to avoid unnecessary branch expansion by operating directly on splices. Other operations. Find invariant. MNNs only have invariants; they can compute variants based on data.
68. Flatten predicate hierarchies by treating dependent variables as indices. Operate on data in topological graph slices, not data structures. To find the invariant not data ops. Then, manipulate, and… the invariant levels (heuristic logic). Prioritise, think of aims, think of the world and logic and … when rewriting rules. I am researching the idea of “any heuristic” to choose data to splice from nondeterministic predicates in my new Detlog programming language.
69. Freedom of expression (connecting anything effortlessly) is the required mathematical standard. To also achieve the correct standard for the simulation, one needs working object science simulations for noninvasive and legal DevOps moves. Prolog. N accessible from nd to d loop. Neurosplice takes data and splices it by interpreting it. It may also involve selectively splicing the data.
70. Cuten (sic) interpreter optimises splices. It also debugs it. Neurosplice takes and interprets. In addition to syntactic and semantic compression, Prolog compresses lines, predicates, algorithms, content, and other dimensions using sentence specs to specify code. Detlog memory problem solved by splicing the data in segments, forgetting memory used by commands with or without output and reusing the same commands, predicates and algorithms but with different data flow in memory. This is a manifestation of quantum memory.
71. Brit-Cov style fg instead of f.g for nested functions in starlog and method chaining in it. Quantify qualifications of mnn manual annealing optimisation correlation, for example, how safe and secure, how many features per level or levels of features, the correlation (Gaussian elimination or algorithmic formula between variables) is. Use neuro techniques to optimise the algorithm when you have all the data to splice for each stage of the algorithm, for example, a subterm with an address for building, processing and outputting (where the subterm with an address optimises and merges processing and outputting). Piglog treats algorithms as data and segments detlog for running on Apple IIe. Converts to a chain with memory variables.
72. Ideas for manual (non-statistical) approximations of regression. Based on cognition. Chatbots should use these methods to guide human and robot cognition and maintain attention. Chatbot/voice smartphone interface. Creating Prolog algorithms and running Prolog. Piglog is effective because it chooses the most likely path rather than just finding the data and path; it considers other paths.
73. Parallel can run the branches better and faster. Complexity is minimised by choosing the appropriate branch when multiple possibilities exist. Pigdetlog runs a predicate if it knows the variables to a high probability, based on their splice data. Splice probability could be calculated from the probability within a list and splice. Separately, Pigdetlog could schedule expensive predicates in regularly available slots. Probability is determined manually, not statistically.
74. In the article, educators and professionals are returning to analog techniques to ensure work is genuinely human. In addition to syntactic and semantic compression, Prolog compresses lines, predicates, algorithms, content, and other dimensions using sentence specs to specify code.
