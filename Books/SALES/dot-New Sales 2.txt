New Sales 2

1. The salesperson modified the main files settings after checking with the user. I assumed all files in a repository were called by the main file when using Lucian CI/CD. Another algorithm worked out which files were the main files. It also worked out the main predicates. It worked out any missing main files and predicates from the existing settings.
2. The salesperson negated the main file settings and scanned all the code for main files and predicates. Any repositories that called a predicate were tested. These predicates were listed in the main file settings. The find dependencies algorithm found these predicates. These predicates required tests and were automatically added to test.
3. The salesperson checked a predicate met a type check. used List Prolog Package Manager dependencies to produce an error if a predicate wasn’t found. When Lucian CI/CD couldn’t find a predicate, it looked around the repositories to find it. Then, Lucian CI/CD added the repository to the list of dependencies. This was done on a predicate not found error.
4. * 
